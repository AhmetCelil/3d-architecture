spring:
  config:
    activate:
      on-profile: local

  datasource:
    url: jdbc:postgresql://localhost:5432/commerce
    username: user
    password: secret123
    driver-class-name: org.postgresql.Driver
